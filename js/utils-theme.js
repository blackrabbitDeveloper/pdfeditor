const themeRoot=document.documentElement;const savedTheme=localStorage.getItem('utils-theme');const initialTheme=savedTheme==='light'||savedTheme==='dark'?savedTheme:(matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light');function setUtilsTheme(theme){themeRoot.dataset.theme=theme;localStorage.setItem('utils-theme',theme);const toggle=document.querySelector('.utils-theme-toggle');if(toggle){toggle.textContent=theme==='dark'?'☀ Light':'◐ Dark';toggle.setAttribute('aria-label',theme==='dark'?'라이트 테마 사용':'다크 테마 사용')}}const themeToggle=document.createElement('button');themeToggle.type='button';themeToggle.className='utils-theme-toggle';themeToggle.addEventListener('click',()=>setUtilsTheme(themeRoot.dataset.theme==='dark'?'light':'dark'));const themeHost=document.querySelector('.header-actions');(themeHost||document.body).prepend(themeToggle);setUtilsTheme(initialTheme);
